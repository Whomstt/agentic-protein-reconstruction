\begin{table}[!tb]
  \centering
  \caption{Agent behaviour across the iteration budget, computed from the per-iteration records. Iteration 1 runs the fixed default levers with no LLM call, so lever-change rates are taken over the LLM-driven iterations only. 'Kept candidate came from iteration 1' is the share of proteins on which no later attempt displaced that deterministic first pass. Setup: \textnormal{\textnormal{\textit{E. coli}}}, 5 digestion replicas.}
  \label{tab:agent_behaviour_ecoli_r5}
  \begin{tabular}{lr}
    \toprule
    Measurement & Value \\
    \midrule
    Iterations per protein (mean) & 5.00 \\
    Distinct lever combinations tried per protein (mean) & 5.00 \\
    LLM-driven iterations (total) & 400 \\
    Kept candidate came from iteration 1 & 7/100 (7.0\%) \\
    Mean iteration of the kept candidate & 2.97 \\
    A later iteration beat iteration 1 on validity & 93/100 (93.0\%) \\
    Changed junction\_window (share of LLM iterations) & 99.8\% \\
    Changed search\_mode (share of LLM iterations) & 33.8\% \\
    Changed beam\_width (share of LLM iterations) & 99.0\% \\
    Changed edge\_mode (share of LLM iterations) & 57.5\% \\
    Changed confirmed\_bonus (share of LLM iterations) & 63.0\% \\
    search\_mode values chosen & beam 76.0\%, greedy 24.0\% \\
    edge\_mode values chosen & hard 68.6\%, soft 31.4\% \\
    \bottomrule
  \end{tabular}
  \par\smallskip\footnotesize{A lever counts as changed when it differs from the previous iteration's value. Percentages of LLM iterations use the total across all proteins as the denominator.}
\end{table}
